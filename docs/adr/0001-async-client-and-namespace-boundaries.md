# ADR 0001: Async client and namespace boundaries

Accepted. One async client composes semantic resource namespaces. Transport owns protocol mechanics.
CLI and future services consume namespaces. No synchronous duplicate protocol implementation.

