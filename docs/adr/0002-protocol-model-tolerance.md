# ADR 0002: Protocol model tolerance

Accepted. Observed required fields validate strongly. Unknown fields, server properties, property
type labels, and SSE event names remain observable. Top-level aggregates retain server time.

